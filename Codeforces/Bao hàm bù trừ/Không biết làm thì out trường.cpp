#include<bits/stdc++.h>

using namespace std;

#define int long long

main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    int a, b, c, d, e, f, g;
    cin >> a >> b >> c >> d >> e >> f >> g;
    cout << a + b + c - d - e - f + g;
}